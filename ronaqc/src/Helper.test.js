import { maxChar, makeConsensus } from './Helper';

test('Check maxChar on pileup strings', () => {
    maxChar('AAAAAA')
  
  });
  

 test('Check maxChar on pileup strings', () => {
    makeConsensus('AAAAAA')
  
  });
